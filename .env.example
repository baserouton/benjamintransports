# Nunca commite credenciais reais.
NODE_ENV=production
PORT=3503
HOST=0.0.0.0

# URL pública do site (obrigatória atrás de Nginx/Cloudflare)
APP_URL=https://benjamintransports.com

# Cookie Secure: true com HTTPS (padrão em production). Use false só em HTTP puro.
# COOKIE_SECURE=true

DATABASE_URL=mysql://usuario:senha@127.0.0.1:3306/banco
SEED_ADMIN_PASSWORD=troque-por-uma-senha-forte
